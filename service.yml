apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: zooplus-image
  name: zooplus-image
spec:
  ports:
    - name: 80-8080
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: zooplus-image
  type: LoadBalancer
status:
  loadBalancer: {}